#pragma once

typedef enum {
	TOKEN_ILLEGAL = 0,
	TOKEN_EOF,

	TOKEN_IDENT,
	TOKEN_INT,

	TOKEN_ASSIGN,
	TOKEN_PLUS,
	TOKEN_MINUS,
	TOKEN_BANG,
	TOKEN_ASTERISK,
	TOKEN_SLASH,

	TOKEN_EQ,
	TOKEN_NEQ,

	TOKEN_LT,
	TOKEN_GT,

	TOKEN_COMMA,
	TOKEN_SEMICOLON,

	TOKEN_LPAREN,
	TOKEN_RPAREN,
	TOKEN_LBRACE,
	TOKEN_RBRACE,

	TOKEN_FUNCTION,
	TOKEN_LET,
	TOKEN_TRUE,
	TOKEN_FALSE,
	TOKEN_IF,
	TOKEN_ELSE,
	TOKEN_RETURN,

	TOKEN_COUNT,
} token_type_t;

typedef struct {
	token_type_t type;
	char *literal;
} token_t;

token_type_t lookup_ident(char *ident);
